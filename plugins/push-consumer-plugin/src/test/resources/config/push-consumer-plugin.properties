# URL on which legacy system can be reached
legacy-system.url=http://localhost:9393/test

received-message-save-file=/tmp/currentPushMessage.xml

service-id=somenode.vessel.push.consumer

service-role=Consumer

service-operation=Push

service-type=VesselService